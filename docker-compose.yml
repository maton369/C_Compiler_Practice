version: "3.8"

services:
  compiler:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: compilerbook
    volumes:
      - ./9cc:/9cc
    working_dir: /9cc
    stdin_open: true
    tty: true
